<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- <link rel="stylesheet" href="/BED_PG/Project-02/public/Stylesheets/index.css"> -->
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="px-10 py-10">
            <form action="/create" method="post">
                <input class="block px-5 py-2 bg-zinc-800 w-full outline-none text-white rounded-md" type="text" placeholder="Title goes here!" name="titles"></input>
                <textarea class="block px-5 py-2 bg-zinc-800 resize-none mt-5 w-full outline-none text-white rounded-md" placeholder="Write here" name="Details"></textarea>
                <input class="bg-blue-500 px-5 py-2 rounded-md px-2 py-2 mt-5 text-white" type="submit" value="Create Task"></input>
            </form>
        </div>
        <div class="tasks flex gap-5 flex-wrap px-10 py-10 w-full">
            <% if(files.length>0){ %>
                <% files.forEach(function(val){ %>
                    <div class="task bg-zinc-800 px-2 py-1 rounded-md w-72">
                        <h1 class="text-white text-3xl tracking-tighter"><%=val%></h1>
                        <a class="text-blue-500 inline-block mt-2" href="/file/<%=val%>">Read More..</a>
                        <a class="text-red-600 block mt-3" href="/deletefile/<%=val%>">Delete!</a>
                    </div>
                <% }) %>
            <% }else{ %>
                <div class=""><h4 class="text-zinc-500">No Tasks are Here...</h4></div>
            <% } %>
        </div>
    </div>
</body>
</html>